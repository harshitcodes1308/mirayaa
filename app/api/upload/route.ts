export async function POST() {
  return Response.json(
    {
      message: "Supabase Storage upload endpoint placeholder. Configure Supabase credentials and bucket policies before enabling writes."
    },
    { status: 501 }
  );
}
